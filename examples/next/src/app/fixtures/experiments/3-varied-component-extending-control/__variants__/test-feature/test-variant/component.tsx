import type { JSX } from "react";
import Variant from "@/components/variant1";
import Control from "../../../component";

const Component = (): JSX.Element => (
  <Variant>
    <Control />
  </Variant>
);

export default Component;
